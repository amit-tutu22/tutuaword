/// App release version — keep in sync with `version` in pubspec.yaml.
const kAppVersion = '26.08.19';

/// Build number — keep in sync with the `+` suffix in pubspec.yaml.
const kAppBuildNumber = '260819';

String get appVersionLabel => 'Version $kAppVersion ($kAppBuildNumber)';
