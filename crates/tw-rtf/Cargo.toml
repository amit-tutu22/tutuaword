[package]
name = "tw-rtf"
description = "RTF import (Phase 3, export-only via DOCX)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tw-model = { path = "../tw-model" }
thiserror = { workspace = true }
